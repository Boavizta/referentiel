nom: Server
type: device 

constants:
  - other_consumption_ratio: 0.15

attributes:
  - server
  - instances_per_server

methods:
  - other : server.other / instances_per_server
  - usage : server.usage / instances_per_server